import { BookFormConfig } from '../../app/book/book-form/book-form-config';

export const bookFormConfig = {
    title: 'Add Book (B)',
    titleStyle: {
        color: '#c6f37c'
    },
    useH2ForTitle: false
} as BookFormConfig;
