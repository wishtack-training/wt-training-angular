export class Book {

    constructor({id, title, price, authorName} = {}) {
        this.id = id;
        this.title = title;
        this.price = price;
        this.authorName = authorName;
    }

}
