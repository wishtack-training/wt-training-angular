describe('SettingsService', () => {
  xit('🚧 should show toolbar', () => {
    // create settingsService
    // check settingsQuery value is toolbar displayed should be false
    // settingsService.showToolbar()
    // check settingsQuery value is toolbar displayed should be true
    throw new Error('🚧 work in progress!');
  });

  xit('🚧 should hide toolbar', () => {
  });
});
