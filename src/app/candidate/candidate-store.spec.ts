describe('CandidateStore', () => {

    xit('should add candidates', () => {
        // 1. new CandidateStore
        // 2. new Candidate x 3 { firstName, lastName, skillList }
        // 3. store.addCandidate x 3
        // 4. store.getCandidateList()
        // 5. check getCandidateList to equal candidate1, 2 & 3
    });

    xit('should remove candidates', () => {
        // 1. new CandidateStore
        // 2. new Candidate x 3 { firstName, lastName, skillList }
        // 3. store.addCandidate x 3
        // 4. store.removeCandidate number 2
        // 5. store.getCandidateList()
        // 6. check getCandidateList to equal candidate1 & 3
    });

    xit('should update candidate', () => {
        throw new Error('😱 Not implemented yet!');
    });

    xit('should filter candidates', () => {
        throw new Error('😱 Not implemented yet!');
    });

});
