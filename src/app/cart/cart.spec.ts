describe('Cart', () => {

    xit('should add sandwich', () => {
        throw new Error('🚧 work in progress!');
    });

    xit('should get total price', () => {
        throw new Error('🚧 work in progress!');
    });

});
